<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="agt.br.com.agt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UsuariosActivity"
            android:label="Lista de Usuarios" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/AlertDialog.AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AlertDialog.AppCompat.TelaCheia" />
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".CadastrarUsuarioActivity"
            android:label="Cadastrar Usuário" />
        <activity
            android:name=".CadastroRotasActivity"
            android:label="Cadastrar Rotas" />
        <activity android:name=".RotasActivity"></activity>
    </application>

</manifest>